#+TITLE: Frozen Supporting Diagnostics

This directory preserves compact evidence used for claims that are not recomputed by the final
QPS--recall sweep.  Historical diagnostic files were copied byte-for-byte on 2026-08-19 from the
=favor-filtered-search= checkout at =/home/ubuntu/cuvs-filter=; raw multi-megabyte traces and result
arrays remain in that checkout.  Deterministic workload statistics and the duplicate-safe YFCC
B0 summary are generated in the isolated checkout from SHA-256-bound inputs.  The final paper
treats all of these as separate diagnostics, not as points in the three-repetition performance
experiment.

=diagnostic_manifest.json= is the machine-readable inventory consumed by the paper staging script.
The script verifies every listed artifact against its SHA-256 before publishing the final-results
sentinel, so stale performance summaries cannot be combined with silently changed diagnostics.

* Accumulator and YFCC discovery evidence

- =PASSING_ACCUMULATOR_DESIGN_AND_EVIDENCE_REPORT.org= documents the accumulator design.  Its
  historical GT-seen versus returned-recall numbers used slot-counted output semantics and are not
  authoritative for the paper's numeric selection-loss claim.  That claim must come from the
  separately generated schema-7, distinct-output YFCC summary.
- =yfcc_b0_distinct_output_summary.json= is that authoritative 1,000-query B0 diagnostic.  It was
  generated from clean commit =69c7945899307567cc5a4b026ea68a47508ee51b= and binds the raw JSON,
  schema-7 query summaries/result arrays, benchmark and library, graph, dataset, queries, GT, and
  predicate metadata by SHA-256.  It reports Base GT-seen =0.6647=, Base returned recall
  =0.5414=, and Retain returned recall =0.6647= under distinct-valid-output set semantics.
  =summarize_yfcc_recall_diagnostic.py= enforces the corresponding configuration, schema,
  correctness, per-query GT-discovery equivalence, and accumulator-completeness gates before
  hashing inputs.  It independently recomputes distinct recall, duplicate rate, and output count
  from the archived result-ID arrays and GT, rather than trusting benchmark counters alone.
  =yfcc_b0_schema7_sources/= is the 876-KiB durable source bundle: raw/config JSON, both schema-7
  manifests and query summaries, both result-ID arrays, and the exact 1,000-query GT matrix.  Each
  file is individually hashed in =diagnostic_manifest.json=.
- =YFCC_RECALL_ROOT_CAUSE_REPORT.org= documents B0 versus continuous deep traversal.
- =workload_selectivity_summary.json= derives exact per-query selectivity distributions directly
  from all 10,000 resident bitmap rows for each workload.  It binds every source manifest, bitmap,
  and fixed-width GT file by SHA-256, including the exact valid-GT-slot totals used in the long
  report.  =summarize_bitmap_selectivity.py= is the corresponding bounded-memory
  derivation tool; =test_summarize_bitmap_selectivity.py= covers continuously packed rows whose
  widths are not word-aligned.  In particular, YFCC's median selectivity is =0.00139485= and
  =0.7116= of queries have selectivity below 1%.
- =yfcc_navix/YFCC_NAVIX_ROOT_CAUSE_REPORT.org=, =diagnostic_summary.json=, and
  =counterfactual_oracle.csv= document the older in-kernel-seeded NaviX diagnostic.  Its oracle is
  a stratified 18-query hard-case sample and must not be described as representative of all YFCC
  queries or as the strict bitmap-seeded method used in the final sweep.

* ArXiv-EMIS locality evidence

=emis_locality/EMIS_LOCALITY_DIAGNOSTIC_REPORT.org= and the three JSON summaries document exact
query-distance locality, graph-edge locality, and traversal counters.  These data motivate a
region-entry hypothesis; they do not by themselves separate seed quality from traversal.  The
final matched-seed sweep is the causal control for that distinction.

* SHA-256

| File | SHA-256 |
|------+----------|
| =PASSING_ACCUMULATOR_DESIGN_AND_EVIDENCE_REPORT.org= | =fe9d8f42dcdd2dc884473484b0d9cbb7fd43bb92e1ca3b4a1c6e1f3a6f9a12c0= |
| =yfcc_b0_distinct_output_summary.json= | =c59b34fcebc8ff34ca9fdd15aa2f5f8303e35d49ccbdfdfc8419e047e528efe1= |
| =summarize_yfcc_recall_diagnostic.py= | =7842ec246290d96339b5cbe63f823fb39363bd6f2caa0db0c57b064b67839ed4= |
| =yfcc_b0_schema7_sources/configs/root_cause_b0_diagnostic.json= | =ea7a0c10403adb7a77d98c1b704febaa707d1ea9e4b7faa27fef50640c60d6f6= |
| =yfcc_b0_schema7_sources/raw/root_cause_b0_diagnostic.json= | =f7e597775e7010d0221f1296dd500821cb4e5b87741aa85bf06f8ccf7024435d= |
| =yfcc_b0_schema7_sources/inputs/groundtruth.ibin= | =fba2e290fae0e88c5f8b8e6a41b357d2c4248b68e93e6c3235bdc2aff003fe1a= |
| =yfcc_b0_schema7_sources/diagnostics/default_b0/manifest.json= | =c5c14e3c9e3277797b57e0541e4f7b79ebbf1ee194fc5949cfd442a43617a6b5= |
| =yfcc_b0_schema7_sources/diagnostics/default_b0/query_summary.csv= | =3611be2795fc612679483b500dd9459f1fa8834091b1fce0eda08ac45ee4fa0a= |
| =yfcc_b0_schema7_sources/diagnostics/default_b0/result_indices.i64bin= | =842bb6627902494afea0c43cb5b9e5bbf792790c3efca6a196eb48968d3717f1= |
| =yfcc_b0_schema7_sources/diagnostics/default_accumulator_b0/manifest.json= | =5c5501ca269d2c3e7a2d6e19d850b43513296bab366553c4ea3f6af5db2ab256= |
| =yfcc_b0_schema7_sources/diagnostics/default_accumulator_b0/query_summary.csv= | =9d7de6137c8ff6864f3d216b4b7409244ede2176c82b69ee61c74a8fed5c4e5e= |
| =yfcc_b0_schema7_sources/diagnostics/default_accumulator_b0/result_indices.i64bin= | =a17c40c44995e6d67bbf57d046c0ab54e586ec563742cdf100edba03311cdbc3= |
| =YFCC_RECALL_ROOT_CAUSE_REPORT.org= | =9cd2d4991503b3a28584a9f4884206785b25f3fabdd95eeaed93f11ab33651d3= |
| =workload_selectivity_summary.json= | =715fdd372fd499488831dc0e8642fd25b1c5b1b429859a3ce1586d6c0339b18c= |
| =summarize_bitmap_selectivity.py= | =891050777c10e941014aa9c5a0cb9f6da2d313dc022f41b1f760d2ff95b5bcec= |
| =test_summarize_bitmap_selectivity.py= | =f49c7f499e2fb63d7f21733a2c3c6defe5bcd0eedce25ff574c08767846839f5= |
| =yfcc_navix/YFCC_NAVIX_ROOT_CAUSE_REPORT.org= | =fe9c6fdc8ca57a063d5fb7ad12bb2ecd9f94dbb3d0b03ff930f2bcc9c0e9ce97= |
| =yfcc_navix/diagnostic_summary.json= | =edd38be020e9d4da780dbcb4e4cec7a29eca75e521f95737085d6a181f284d5f= |
| =yfcc_navix/counterfactual_oracle.csv= | =98bd435d80f4935cf79718dfad9312dd15cbaa10aeda35da49219992b56e659a= |
| =emis_locality/EMIS_LOCALITY_DIAGNOSTIC_REPORT.org= | =e7a1b7dca91ffde75acf41a82e219bdf35ace42847b1a7cb2ae9e4c2682f5130= |
| =emis_locality/exact_summary.json= | =ae1b5424939d3a0c374ace73c71c6bc245719de1f2870b0531c35a3363e67976= |
| =emis_locality/graph_summary.json= | =4edadd37841bce82d83dc2ed9763a1bf1ee04231b518b9bf4fd7169c3a86f336= |
| =emis_locality/traversal_summary.json= | =173d84ab36e2c4a9f4a245f5630eb108ad1dae23fe9897ee13088f5a11ff81d4= |
